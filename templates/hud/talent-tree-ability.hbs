<div class="line-item">
    <header class="flexrow">
        <div class="title">
            <h2>{{label}}</h2>
        </div>
    </header>
    {{#if descriptionHTML}}
    <section class="description">{{{descriptionHTML}}}</section>
    {{/if}}
</div>
