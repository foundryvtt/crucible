<div class="feature-item line-item talent" data-uuid="{{uuid}}" data-link data-tooltip>
    <img class="icon" src="{{img}}" alt="{{name}}">
    <div class="title">
        <h4>{{name}}</h4>
        {{#if tags}}
        <div class="tags">
            {{#each tags}}
            <span class="tag">{{this}}</span>
            {{/each}}
        </div>
        {{/if}}
    </div>
</div>
